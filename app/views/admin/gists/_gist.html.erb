<tr>
  <td><%= link_to gist.question.body[0..25] + '...', admin_gist_path(gist) %></td>
  <td><%= gist.author.email %></td>
  <td><%= link_to t('.github_url'), gist.url %></td>
  <td><%= link_to t('links.show'), admin_gist_path(gist) %></td>
</tr>